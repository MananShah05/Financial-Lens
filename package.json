{
  "name": "financial-time-series",
  "version": "1.0.0",
  "description": "Financial Time Series Scenario & Sensitivity Analysis",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend"
  }
}
